I will implement the Level-2 Evidence-grounded LiteratureReview as requested.

### 1. Update Documentation
- **Edit `README.md`**: Add `pypdf` to the dependency list as requested.

### 2. Extend Research State
- **Edit `sr_pipeline/state.py`**: Add new fields to `ResearchState` to store the required artifacts:
  - `annotated_bib` (List[Dict])
  - `evidence_table` (List[Dict])
  - `missing_matrix` (List[Dict])

### 3. Implement Literature Module (`sr_pipeline/literature/`)
I will create a new package `sr_pipeline/literature/` to contain the logic, keeping it modular and organized.
- **`ingest.py`**:
  - Implement `paper_fetch(sources)`: Handles local paths (and optional URLs), manages `data/papers_cache/`, and maintains `manifest.json`.
  - Implement `pdf_parse(cached_path)`: Uses `pypdf` to extract text and sections deterministically.
- **`corpus.py`**:
  - Implement `Chunk` dataclass.
  - Implement `Corpus` class for chunking (800-1200 chars, 150 overlap) and TF-IDF-ish retrieval.
- **`agent.py`**:
  - Implement `LiteratureReviewAgent` (or logic) that:
    1.  Ingests papers.
    2.  Builds a corpus.
    3.  Generates `annotated_bib` (using retrieval + summarization).
    4.  Generates `evidence_table` (retrieving evidence for generated claims).
    5.  Generates `missing_matrix` (identifying gaps).

### 4. Integrate into Pipeline
- **Edit `sr_pipeline/stages.py`**:
  - Update `LiteratureStage.run()` to delegate to the new `sr_pipeline.literature` logic.
  - Ensure it saves the 3 artifacts to the `ResearchState` and writes them to JSON files in the output directory.

### 5. Implement Audit Gates
- **Edit `audit_logs.py`**:
  - Add Level-2 checks:
    - **L2A**: `evidence_table.json` exists and is not empty.
    - **L2B**: Every claim in `evidence_table` has >= 1 support snippet.
    - **L2C**: Every support snippet has provenance fields (paper_id, section, etc.).

### 6. Add Acceptance Test
- **Edit `test_pipeline.py`**:
  - Add `run_level2_test()`:
    - Embeds a minimal valid PDF (base64).
    - Writes it to a temp file.
    - Runs the `LiteratureStage` (or a mini-pipeline focused on it).
    - Verifies the artifacts and audit pass.
  - Ensure existing PASS/FAIL tests still work.

### 7. Self-Iteration & Verification
- Run `python test_pipeline.py` to verify all gates pass.
- Ensure `git status --porcelain` is clean (ignoring cache/outputs).
- Produce the final `ACCEPTANCE_JSON` and report.
